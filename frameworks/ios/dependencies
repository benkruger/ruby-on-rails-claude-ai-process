#!/usr/bin/env bash
# FLOW dependency updater for iOS
# Created by /flow:flow-prime — edit to match your project
set -euo pipefail

REPO_ROOT="$(cd "$(dirname "$0")/.." && pwd)"

if [ -f "$REPO_ROOT/Package.swift" ]; then
    xcodebuild -resolvePackageDependencies
else
    echo "No Package.swift found — no external dependencies to resolve."
fi
